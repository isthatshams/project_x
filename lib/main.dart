import 'package:flutter/material.dart';
import 'package:project_x/screens/SignIn.dart';

void main() {
  //runApp(const HomePage());
  runApp(const MaterialApp(debugShowCheckedModeBanner: false, home: SignIn()));
}
